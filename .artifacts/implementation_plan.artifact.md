# 계산기 UI 개선 및 데이터 시각화 고도화 계획

사용자별로 입력된 원천 데이터(수당, 월급)와 그에 따른 배분 결과를 한눈에 비교할 수 있도록 계산기 하단 표 구성을 전면 개편합니다.

## Proposed Changes

### 1. 데이터 구조 확장
#### [MODIFY] [CalculPage](file:///C:/Users/nalb/OneDrive/바탕 화면/최종본/myhome-web-main/app/calcul/page.tsx)
*   `Budget` 인터페이스에 `allowance(5일 수당)`, `salary(20일 월급)`, `totalSalary(합계)` 필드를 추가합니다.
*   `fetchUserBudgets` 함수에서 Firestore의 원본 데이터를 모두 가져와 매핑하도록 수정합니다.
*   '계산하기' 클릭 시, 현재 입력된 값이 표에 즉시 반영되어 미리 볼 수 있도록 로직을 보완합니다.

### 2. 이중 테이블 UI 구현
#### [MODIFY] [BudgetComparisonTable](file:///C:/Users/nalb/OneDrive/바탕 화면/최종본/myhome-web-main/app/calcul/components/BudgetComparisonTable.tsx)
*   기존 하나의 표를 두 개의 독립된 테이블로 분리합니다.
*   **테이블 1 (원천 데이터)**: 사용자별 '5일 수당', '20일 월급', '합계'를 표시합니다.
*   **테이블 2 (배분 결과)**: 사용자별 '생활비', '적금', '투자', '가족' 배분액을 표시합니다.
*   기존의 디자인(폰트, 색상, 테두리 등)을 완벽하게 유지하여 일질감 없는 UI를 제공합니다.

## Verification Plan

### Manual Verification
1.  **데이터 입력**: 'bak' 계정으로 접속하여 수당 100만 원, 월급 300만 원 입력 후 '계산하기' 클릭.
2.  **표 확인**:
    *   상단 표에 'bak', '1,000,000', '3,000,000', '4,000,000'이 뜨는지 확인.
    *   하단 표에 계산된 배분액(25%, 25%, 15%, 10%)이 정확히 뜨는지 확인.
3.  **데이터 저장**: '저장' 버튼 클릭 후 페이지를 새로고침했을 때, 저장된 데이터가 두 표에 모두 유지되는지 확인.
4.  **배우자 데이터 비교**: 다른 브라우저에서 'yong' 계정으로 데이터를 입력했을 때, 두 사용자의 데이터가 위아래 표에서 각각 행으로 비교되는지 확인.
